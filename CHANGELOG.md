# 更新记录

## 0.2.0 — 2026-09-06

本版集中修复语音生命周期和本地音频访问安全，保留现有触控、固定按键和豆包输入方式。

### 修复

- App 与 Capture 改为双向签名校验的 XPC 通信，使用内核提供的调用方身份，不再信任通知中的 PID。
- 音频共享内存仅允许 root 和 CoreAudio 服务组访问；普通本地进程不能直接读取或改写遥控器 PCM。
- 停止或异常退出时先撤销音频会话，再回收采集进程；增加 generation 和有期限的音频 lease，避免旧音频继续播放或串入下一句。
- 虚拟麦克风先于采集服务启动时，会在非实时线程重试连接，无需重新打开麦克风。
- 收尾阶段再次按住 Siri 键会接续下一次录音，不再丢弃新的按下。
- 松开 Siri 键后按最后一帧到达时间等待尾音，随后限时排空，再释放 Fn。

### 验证与维护

- App 直接使用经过单元测试的豆包会话状态机，新增快速点按、连续会话、超时、失效回调和异常释放测试。
- CI 新增 HAL 和 Router 检查，覆盖音频契约、多客户端读取、PacketLogger 解析，以及 ASan/UBSan 生命周期测试。
- 新增只读的 `--verify-capture` 诊断入口，检查 App 与 Capture 的真实签名握手，不启动录音或发送按键。
- 更新安装审计、卸载清理和开发文档。

### 升级说明

- 请使用 `SiriRemote-0.2.0-Full-Setup.pkg` 完整升级，无需先卸载；现有用户配置保留。
- 本版升级了共享音频协议，App、Capture、Router 和 HAL 必须一起更新，不能只复制 App。
- 安装和完整卸载包均使用 Developer ID Installer 签名，内含组件使用 Developer ID Application 签名，带可信时间戳；**尚未经过 Apple 公证**。
- PacketLogger 仍需单独安装，应用仍不修改 macOS 默认输入设备。
- 本次预编译安装包面向 Apple Silicon（arm64），需要 macOS 13 或更高版本。

## 0.1.0 — 2026-09-03

首个公开版本：A2854 触控与固定按键、按住 Siri 调用豆包语音、中文/英文界面和可选开机启动。
